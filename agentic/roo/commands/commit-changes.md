 Create a git commit message summarizing the changes. 
 Commit the changes to git using that message.
 Include new and untracked files in the commit. Add these files if required.
 When pushing to remoate set the upstream to remote origin as part of the push command.
 ASK BEFORE PUSHING CHANGES TO REMOTE.
 